// =============================================================================
// Completions Module - Dynamic completion providers for CLI arguments
// =============================================================================

export {
  type CompletionItem,
  type CompletionProvider,
  getAgentNames,
  getInterjectionIds,
  getQuestionIds,
  getRepoNames,
  getTaskIds,
  getTaskStatuses,
} from "./providers";
