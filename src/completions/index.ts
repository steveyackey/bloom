// =============================================================================
// Completions Module - Dynamic completion providers for CLI arguments
// =============================================================================

export {
  getTaskStatuses,
  getTaskIds,
  getRepoNames,
  getAgentNames,
  getQuestionIds,
  getInterjectionIds,
  type CompletionItem,
  type CompletionProvider,
} from "./providers";
